import React from 'react';
import ReactDOM from 'react-dom';
// import main from './src/main/body';

const app =  document.getElementById('app');

// ReactDOM.render(que voy a renderiar, donde lo haré)
const contect=<h1>hola mundo</h1>;
ReactDOM.render(contect, app)